# ApplicationSet: inferflow-auto-sync
# Manages environments that use automated sync (dev).
# Separate from inferflow-manual-sync to allow independent pause/resume.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: inferflow-auto-sync
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - env: dev
            namespace: inferflow-dev
            targetRevision: main
  template:
    metadata:
      name: "inferflow-{{ .env }}"
      namespace: argocd
      labels:
        app.kubernetes.io/name: inferflow
        env: "{{ .env }}"
    spec:
      project: inferflow
      source:
        repoURL: https://github.com/jgallego9/inferflow-mlops.git
        targetRevision: "{{ .targetRevision }}"
        path: infra/helm/inferflow
        helm:
          valueFiles:
            - values.yaml
            - "values-{{ .env }}.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
---
# ApplicationSet: inferflow-manual-sync
# Manages environments that require human approval before sync (staging, prod).
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: inferflow-manual-sync
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - env: staging
            namespace: inferflow-staging
            targetRevision: main
          - env: prod
            namespace: inferflow-prod
            targetRevision: release/stable
  template:
    metadata:
      name: "inferflow-{{ .env }}"
      namespace: argocd
      labels:
        app.kubernetes.io/name: inferflow
        env: "{{ .env }}"
    spec:
      project: inferflow
      source:
        repoURL: https://github.com/jgallego9/inferflow-mlops.git
        targetRevision: "{{ .targetRevision }}"
        path: infra/helm/inferflow
        helm:
          valueFiles:
            - values.yaml
            - "values-{{ .env }}.yaml"
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{ .namespace }}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
        # No automated block — sync requires explicit human/CI approval.
