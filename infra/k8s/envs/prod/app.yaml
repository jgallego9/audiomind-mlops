# infra/k8s/envs/prod/app.yaml
# Source of truth for the 'prod' environment.
env: prod
namespace: inferflow-prod
targetRevision: release/stable # tracks a protected release branch — requires PR approval
helmChartPath: infra/helm/moiraweave
autoSync: false               # manual approval required before production sync
