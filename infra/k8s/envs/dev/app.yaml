# infra/k8s/envs/dev/app.yaml
# Source of truth for the 'dev' environment.
# Consumed by infra/k8s/argocd/applicationset.yaml (Git File generator).
env: dev
namespace: inferflow-dev
targetRevision: main          # tracks HEAD of main — latest build always deploys
helmChartPath: infra/helm/inferflow
autoSync: true                # automated sync + self-heal for dev
