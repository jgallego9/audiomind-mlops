apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "audiomind.apiGateway.fullname" . }}
  labels:
    {{- include "audiomind.labels" . | nindent 4 }}
    app.kubernetes.io/component: api-gateway
spec:
  replicas: {{ .Values.apiGateway.replicaCount }}
  selector:
    matchLabels:
      {{- include "audiomind.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api-gateway
  template:
    metadata:
      labels:
        {{- include "audiomind.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api-gateway
    spec:
      serviceAccountName: {{ include "audiomind.serviceAccountName" . }}
      containers:
        - name: api-gateway
          image: "{{ .Values.apiGateway.image.repository }}:{{ .Values.apiGateway.image.tag }}"
          imagePullPolicy: {{ .Values.apiGateway.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            # --- sourced from values ---
            {{- range $key, $value := .Values.apiGateway.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            # --- internal service URLs derived from release name ---
            - name: REDIS_URL
              value: {{ include "audiomind.redisUrl" . | quote }}
            - name: QDRANT_URL
              value: {{ include "audiomind.qdrantUrl" . | quote }}
            # --- secret values ---
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.apiGateway.existingSecret }}
                  key: {{ .Values.apiGateway.secretKey }}
          livenessProbe:
            {{- toYaml .Values.apiGateway.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.apiGateway.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.apiGateway.resources | nindent 12 }}
